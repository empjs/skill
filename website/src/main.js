import './style.css'

// Simple interaction for copy button if not inline
document.querySelectorAll('.copy-btn').forEach(btn => {
  btn.addEventListener('click', () => {
    // logic handled in html onclick for simplicity in this static page
  })
})
